<UserControl x:Class="SCG.SINOStock.Views.ToolView.CheckBoxButtonList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="280" d:DesignWidth="500">
    <Border BorderBrush="Black" BorderThickness="1,1,1,1" > 
        <Grid x:Name="gridMain">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                <Label Content="品质信息选择：" />
                <ScrollViewer  Width="270" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="spMain" Orientation="Horizontal">
                    </StackPanel>
                   
                </ScrollViewer>
                <Button Content="确定" Height="25" Width="60" Click="Button_Click_1"/>
                <Label Content="" />
                <CheckBox x:Name="cbAll" Content="全选" VerticalContentAlignment="Center" Click="cbAll_Click"/>
            </StackPanel>
            <UniformGrid x:Name="ufgMain" Grid.Row="1">

            </UniformGrid>
        </Grid>
    </Border>
</UserControl>
